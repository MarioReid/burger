<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newdevour=""="{{devoured}}">
		{{#if devoured}}DEVOUR TIME!{{else}}NOT DEVOURED!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
